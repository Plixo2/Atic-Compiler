

struct Position {
    from: number;
    to: number;
}

struct AticError {
    position: Position;
    msg: string;
}

fn newPosition(from: number, to: number) -> Position {
    return { from = from, to = to };
}